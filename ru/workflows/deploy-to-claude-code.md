# Деплой в Claude Code

Пошаговая инструкция по деплою правил из этого репозитория в Claude Code (Anthropic).

---

## Отличия от Cursor

| Параметр | Cursor | Claude Code |
|----------|--------|-------------|
| Основной файл | `.cursorrules` | `CLAUDE.md` |
| Доменные правила | `.cursor/rules/*.mdc` с globs | `.claude/rules/` (иерархия папок) |
| Активация по globs | да | нет — всё через размещение файлов |
| alwaysApply | да | нет |

В Claude Code нет автоматической загрузки правил по типу файла. Управление контекстом — через иерархию `CLAUDE.md`: глобальный (`~/.claude/CLAUDE.md`), в корне проекта (`CLAUDE.md`), в подпапках.

---

## Шаг 1. Ответьте на вопросы по настройке

### 1.1 Уровень применения

**Вопрос:** Вы хотите применить правила:
- **Глобально** (для всех проектов) → файлы в `~/.claude/`
- **Для конкретного проекта** → файлы в корне проекта
- **Для подпапки проекта** → файлы в подпапке

### 1.2 Доменные правила

Посмотрите `ru/rules/domain/`. Для Claude Code доменные правила не загружаются автоматически — их нужно либо включить в основной `CLAUDE.md`, либо разместить в `.claude/rules/` и ссылаться на них вручную.

**Вопрос:** Вы хотите включить доменные правила прямо в `CLAUDE.md` (проще) или организовать их в `.claude/rules/` (чище для большого числа правил)?

---

## Шаг 2. Создайте CLAUDE.md

**Для глобального применения:**
```bash
mkdir -p ~/.claude
```
Создайте `~/.claude/CLAUDE.md`

**Для проекта:**
Создайте `CLAUDE.md` в корне проекта.

---

## Шаг 3. Заполните CLAUDE.md

Используйте скилл `deploy-elucidating-rules` (файл `ru/skills/deploy-elucidating-rules/SKILL.md`) — он автоматически создаёт файлы и очищает содержимое от служебных строк (дат создания и актуализации).

Если деплоите вручную: скопируйте в `CLAUDE.md` содержимое нужных файлов из `ru/rules/` и **удалите строки `*Дата создания:*` и `*Дата актуализации:*`** — они служебные и не должны попадать в правила.

---

## Шаг 4. Проверьте работоспособность

```bash
claude   # запуск в папке проекта
# затем спросить: "Какие правила у тебя есть?"
```

---

## Обновление

При выходе новой версии:
1. `git pull` в папке ElucidatingYourLLM
2. Проверить CHANGELOG.md
3. Перенести нужные изменения в `CLAUDE.md`
